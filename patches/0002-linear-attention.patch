# Patch: Attention alternatives — Linear, Mamba, xLSTM
#
# The three O(T) attention replacement modules are stored as plain Python
# files in this directory (linear_attention.py, mamba_attention.py,
# xlstm_attention.py) and copied into Megatron-LM by the sbatch setup
# rather than via git apply, to avoid unified-diff line-count fragility.
#
# gpt_layer_specs.py is patched by patches/patch_layer_specs.py (Python).
#
# Applied to: Megatron-LM core_v0.16.1
